<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/counterbutton"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <TextView
        android:id="@+id/counterTitle"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_toLeftOf="@+id/bGTC"
        android:gravity="center"
        android:paddingTop="10dp"
        android:text="@string/layoutTitle"
        android:textAllCaps="true"
        android:textSize="20sp" />

    <Button
        android:id="@+id/bGTC"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:onClick="detailsButton"
        android:text="@string/sSTATS" />

    <TextView
        android:id="@+id/counterCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/bGTC"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/bGTC"
        android:gravity="center"
        android:text="@string/layoutCount" />

</RelativeLayout>